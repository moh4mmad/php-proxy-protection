# php-proxy-protection
Detect user who is using proxy connection

### Installation
add this line to php file (config file recommended to secure all pages) .
```
include("proxy-detect.php");
```
### Requirements
PHP 5.x or higher version
